<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.bskup.lyra.MainActivity">

    <TextView
        android:id="@+id/library_text_view"
        style="@style/TextViewStyle"
        android:text="@string/library_info_text"/>

    <Button
        android:id="@+id/library_home_button"
        style="@style/ButtonStyle"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/library_text_view"
        android:text="Home"/>

    <Button
        android:id="@+id/library_lyrics_button"
        style="@style/ButtonStyle"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/library_home_button"
        android:text="Lyrics"/>

    <Button
        android:id="@+id/library_settings_button"
        style="@style/ButtonStyle"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/library_lyrics_button"
        android:text="Settings"/>

    <Button
        android:id="@+id/library_album_button"
        style="@style/ButtonStyle"
        android:layout_alignParentRight="true"
        android:layout_below="@id/library_text_view"
        android:text="Go To Album"/>

    <Button
        android:id="@+id/library_artist_button"
        style="@style/ButtonStyle"
        android:layout_alignParentRight="true"
        android:layout_below="@id/library_album_button"
        android:text="Go To Artist"/>

</RelativeLayout>
